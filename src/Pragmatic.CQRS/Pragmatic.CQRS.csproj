<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>true</IsPackable>
    <RootNamespace>Pragmatic.CQRS</RootNamespace>

    <!-- Package Identity -->
    <PackageId>Pragmatic.CQRS</PackageId>
    <Authors>Tristan Rhodes</Authors>
    <Company>Pragmatic Systems</Company>
    <Description>A lightweight, MIT-licensed CQRS mediator for .NET with MediatR-compatible interfaces. Provides core support for request/response handlers, void (fire-and-forget) handlers, notification fan-out, and pipeline behaviors.</Description>
    <PackageTags>cqrs;mediator;mediatr-alternative;lightweight;mit</PackageTags>

    <!-- Package Links -->
    <PackageProjectUrl>https://github.com/pragmatic-systems/Pragmatic.CQRS</PackageProjectUrl>
    <RepositoryUrl>https://github.com/pragmatic-systems/Pragmatic.CQRS.git</RepositoryUrl>
    <RepositoryType>git</RepositoryType>

    <!-- Package Content -->
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="8.0.2" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="8.0.2" />
  </ItemGroup>

  <!-- Install Husky.Net githooks on restore. Forces secret scan / format. -->
  <Target Name="RestoreDotnetTools" BeforeTargets="Restore;CollectPackageReferences" Condition="'$(HUSKY)' != '0'">
    <Exec Command="dotnet tool restore" WorkingDirectory="$(MSBuildThisFileDirectory)..\.." ContinueOnError="true" />
    <Exec Command="dotnet husky install" WorkingDirectory="$(MSBuildThisFileDirectory)..\.." ContinueOnError="true" />
  </Target>

</Project>
